# Dashboard logos

- **`Logotype.svg`** — source artwork (edit here).
- **`public/dashboard/logos/Logotype.svg`** — copy used by the app (Next.js serves static files from `public/`). After changing the file here, copy it to `public/dashboard/logos/Logotype.svg` so the header updates.
